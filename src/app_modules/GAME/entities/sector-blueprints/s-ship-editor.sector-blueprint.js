import { SectorBlueprint, Ship } from 'GAME/classes';
import * as shipBlueprints from 'GAME/entities/ship-blueprints';

export var sShipEditorSectorBlueprint = new SectorBlueprint({
    name  : 'Ship Editor',
    sizeX : 600,
    sizeY : 600
});
